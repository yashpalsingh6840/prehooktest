# SyntheticScriptComponentSeams

Generated by `ssisx generate` from `SyntheticScriptComponentSeams.dtsx`. Regenerated every run --
hand edits to this file are discarded; a Tier-1/2 fill belongs under `fills/` (see `HOW-TO-FILL-GAPS.md` at the `--out` root), never in this file.

**Generated code here is never verified against a real database or real data in this
environment.** `dotnet build` proves it compiles; `dotnet test --filter
Category!=Integration` proves the starter tests pass against fakes (`TestDoubles/`, no
SQL Server involved). Neither proves the SQL/business logic is correct against a real
target -- that needs a human, or an `Integration`-tagged run with real connectivity.

## Control flow

Seq follows real execution order (the same topological order the generated `RunAsync`
calls these methods in). "Concurrency" names a wave of steps SSIS ran with no ordering
constraint between them -- each such wave runs via `Task.WhenAll`, each branch in its own
transaction (see `{Package}.cs`'s own `RunBranchAsync`).

| Seq | SSIS task | Container | Concurrency | Generated method (full signature) | Test | Gap |
|---|---|---|---|---|---|---|
| 1 | DFT_Load | _(root)_ | wave 0 | `internal async Task DFT_Load(IUnitOfWork uow, CancellationToken ct)` | `DFT_LoadStepDataFlowTests.cs` -- DataFlowTask: reconstructs the flow's real source/transform/sink and asserts at least one row was bulk-inserted; needs real connectivity (a SQL/Excel source somewhere in the flow), so this is Integration-tagged. | OLE DB Source 'OLE DB Source' uses a SqlCommand -- generated code assumes each result-set column is named/aliased exactly like the pipeline's own buffer column name; verify before running. This tool never rewrites the author's own SQL. |

## Data flow detail

Per Data Flow Task, every pipeline component in real data-flow order
(`Ssis.Extract.Dtsx.PipelineFlowOrder`), with what it does
(`Ssis.Extract.Dtsx.ComponentInsight.Describe`) -- the same two functions the
SsisValidationKit walkthrough reads, so a Seq number here means the same thing there.
A component folded straight into a transform (Derived Column, Data Conversion, Sort,
Union All, Row Count) has no generated method of its own -- it contributes to whichever
source/sink method's own row-mapping logic consumes it, not a separate row here.

### DFT_Load

| Seq | Component | Type | What it does |
|---|---|---|---|
| 1 | OLE DB Source | Microsoft.OLEDBSource | Reads `SELECT ID, FirstName, LastName FROM dbo.SyntheticScriptSeamsInput` via `CM_Sql` |
| 2 | SCR_Cleanse | Microsoft.ManagedComponentHost | Script (CSharp): 1 source file(s) |
| 3 | OLE DB Destination | Microsoft.OLEDBDestination | Writes `[dbo].[SyntheticScriptSeamsTarget]` via `CM_Sql` (fast load) |

## Data flow sources and sinks

Each Data Flow Task's own source/sink methods, constructed directly inside the step
method above that calls them -- no DI factory indirection between where a thing is
registered and where it runs.

| Kind | SSIS component / entity | Generated method (full signature) |
|---|---|---|
| Source | OLE DB Source | `internal IRowSource<SyntheticScriptSeamsTargetSqlRow> OLEDBSource(IUnitOfWork uow)` |
| Sink | OLE DB Destination | `internal IBulkSink<SyntheticScriptSeamsTarget> OLEDBDestination()` |

## Test coverage notes

Read this before adding a test -- it says what each existing test already asserts, so a
new test adds real coverage instead of duplicating what's here. Additional tests go under
`fills/<Package>/MoreTests/` and are applied with `ssisx apply-tests` -- see
`HOW-TO-FILL-GAPS.md` at the `--out` root. **This is NOT a gap** -- the package is already
fully generatable without it.

| Seq | Test | Kind | What's already asserted |
|---|---|---|---|
| — | `RunAsyncTests.cs` | RunAsync | asserts the failure path (a fake throws on a chosen step -> rollback, failure handlers ran, notifier saw failure) and, when every source is file-based, the happy path (step count/order, per-branch transactions); does not vary input data beyond the fixed sample/rows. |
| — | `SyntheticScriptSeamsTargetSinkTests.cs` | Sink-Sql | writes 2 synthesized entities through the fake unit of work; asserts DestinationTable + ColumnMappings only, never a real INSERT. |
| — | `OLEDBSourceSourceTests.cs` | Source-Sql | asserts `.Name` only by default; the real read is `[Trait("Category","Integration")]`, run only against a reachable server. |
| 1 | `DFT_LoadStepDataFlowTests.cs` | DataFlowTask | reconstructs the flow's real source/transform/sink and asserts at least one row was bulk-inserted; needs real connectivity (a SQL/Excel source somewhere in the flow), so this is Integration-tagged. |

## Where things live

- `TestDoubles/` (in `{Package}.Tests/`) -- `FakeUnitOfWork`/`RecordingNotifier`/
  `PackageHarness`, generated fresh for this package. No SQL Server involved:
  `dotnet test --filter Category!=Integration` never opens a real connection.
- `gaps/<Package>/` (under `--out` root) -- one self-contained work packet per Tier-1/2
  gap this package reported; `gaps.json` is the index.
- `fills/`/`fills-library/` -- human-authored answers, never written by `ssisx`; applied
  via `ssisx apply-fills`.
- `appsettings.Development.json` + `TestData/` -- a package-relative sample-data override
  for local `dotnet run` (`DOTNET_ENVIRONMENT=Development`); `TestData/` is empty until a
  `LOCAL-DATA` fill supplies real files.

## Gaps reported for this package

| Location | Reason | Blocking |
|---|---|---|
| DFT_Load | OLE DB Source 'OLE DB Source' uses a SqlCommand -- generated code assumes each result-set column is named/aliased exactly like the pipeline's own buffer column name; verify before running. This tool never rewrites the author's own SQL. | no |
| SyntheticScriptSeamsTarget.SCR_Cleanse | Script Component 'SCR_Cleanse' produces 2 column(s) (FullName, IsValid) -- emitted as a single `private partial SCR_CleanseResult SCR_Cleanse(SyntheticScriptSeamsTargetSqlRow row, in RowContext ctx)` seam. The project will not compile (CS8795) until a second part of 'SyntheticScriptSeamsTargetTransform' implements it; apply one with `ssisx apply-fills`. | yes |
| SyntheticScriptSeamsTarget.SCR_Cleanse | Script Component 'SCR_Cleanse' (SCR_Cleanse) has no generated test at all (a filled seam is human logic -- see the TEST-ORACLE work packet to write one). | no |
| SyntheticScriptComponentSeams.Notification | no notification wiring was generated (IPackageResultNotifier/AddEmailNotifications) -- a .dtsx carries no notification-recipient information at all, so this is opt-in; re-run with --notifications once real recipients/SMTP settings exist | no |

